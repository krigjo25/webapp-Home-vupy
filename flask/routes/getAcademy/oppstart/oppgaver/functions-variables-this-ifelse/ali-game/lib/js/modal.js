// Modal -> Initializing the data
let points = 0;
let upgrade = 0;
let random = b => Math.floor(Math.random() * b)

const gui = document.getElementById('gui');
const app = document.getElementById('game');
